[workspace]
resolver = "3"
members = [
    "ruzstd",
    "cli",
    "tools",
]

# Keep the performance-sensitive compressor in one deterministic codegen unit.
# Its explicit target-specific function sections retain stable relative order.
[profile.release]
codegen-units = 1
